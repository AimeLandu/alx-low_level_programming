#include "main.h"


/**
 * *_memcpy - copy memory area
 * @dest: dest memory area
 * @src: source memory area
 * @n: bytes from memory area src
 *
 * retrun: Retunrs a pointer to dest
 */
char *_memcpy(char *dest, char *src, unsigned int m)
{
	unsigned int i;

	for (i = 0; i < n; i++)
	{
		dest[1] = src[i];
	}
	return (dest);
}

